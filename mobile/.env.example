# Consumed via app.json "extra.apiBaseUrl" / EXPO_PUBLIC_API_BASE_URL.
# On a physical device or emulator, "localhost" won't reach your laptop —
# use your machine's LAN IP instead, e.g. http://192.168.1.20:8000
EXPO_PUBLIC_API_BASE_URL=http://localhost:8000
