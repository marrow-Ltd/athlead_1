"""
Placeholder auth tests. Wire up a test DB (e.g. via a pytest fixture that
creates a temp Postgres schema or uses testcontainers) before running.
"""
import pytest


@pytest.mark.skip(reason="Requires a configured test database — see README for setup.")
def test_register_and_login():
    pass
